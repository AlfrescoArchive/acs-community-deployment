# Defines the deployment for the core-aio transformer app
# Details: https://git.alfresco.com/Repository/alfresco-docker-transformers
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: {{ template "content-services.shortname" . }}-core-aio
  labels:
    app: {{ template "content-services.shortname" . }}-core-aio
    chart: {{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
    component: transformers
spec:
  replicas: {{ .Values.core-aio.replicaCount }}
  template:
    metadata:
      labels:
        app: {{ template "content-services.shortname" . }}-core-aio
        release: {{ .Release.Name }}
        component: transformers
    spec:
      {{- if .Values.global.alfrescoRegistryPullSecrets }}
      # only set this secret if a private docker registry variable is defined
      imagePullSecrets:
        - name: {{ .Values.global.alfrescoRegistryPullSecrets }}
      {{- end }}
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
          - weight: 10
            podAffinityTerm:
              labelSelector:
                matchExpressions:
                - key: app
                  operator: In
                  values:
                  - {{ template "content-services.shortname" . }}-core-aio
              topologyKey: failure-domain.beta.kubernetes.io/zone
          - weight: 5
            podAffinityTerm:
              labelSelector:
                matchExpressions:
                - key: app
                  operator: In
                  values:
                  - {{ template "content-services.shortname" . }}-core-aio
              topologyKey: kubernetes.io/hostname
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.core-aio.image.repository }}:{{ .Values.core-aio.image.tag }}"
          imagePullPolicy: {{ .Values.core-aio.image.pullPolicy }}
          envFrom:
          - configMapRef:
              # config map to use, defined in config-core-aio.yaml
              name: {{ template "content-services.shortname" . }}-core-aio-configmap
          ports:
            - containerPort: {{ .Values.core-aio.image.internalPort }}
          resources:
          readinessProbe:
            httpGet:
              path: /ready
              port: {{ .Values.core-aio.image.internalPort }}
            initialDelaySeconds: {{ .Values.core-aio.readinessProbe.initialDelaySeconds }}
            periodSeconds: {{ .Values.core-aio.readinessProbe.periodSeconds }}
            timeoutSeconds: {{ .Values.core-aio.readinessProbe.timeoutSeconds }}
          livenessProbe:
            httpGet:
              path: /live
              port: {{ .Values.core-aio.image.internalPort }}
            initialDelaySeconds: {{ .Values.core-aio.livenessProbe.initialDelaySeconds }}
            periodSeconds: {{ .Values.core-aio.livenessProbe.periodSeconds }}
            failureThreshold: 1
            timeoutSeconds: {{ .Values.core-aio.livenessProbe.timeoutSeconds }}
{{ toYaml .Values.core-aio.resources | indent 12 }}
